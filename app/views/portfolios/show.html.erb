<% @page_header = "Portfolio :: #{@portfolio.title}" %>

    <div class="row">
      <div class="col-md-7">
       <%= image_tag("#{@portfolio.portfolio_image.url}") %>
      </div>



      <div class="col-md-5">
        <h4><%= link_to "#{@portfolio.title}", '#' %></h4>
        <p><%= @portfolio.description %></p>
        <p><%= link_to 'View website', @portfolio.website %></p>
      </div>

    </div>
